// Online C compiler to run C program online
#include <stdio.h>

int main()
{
    int p;
    int v[20];
    int menor, maior;
    
    
    for(p=0;p<=2;p++)
    {
     printf("digite o valor:");
     scanf("%d",&v[p]);
     
     if (p == 0){
         maior=v[0]
         menor=[0]
     }
     else {
        if (v[p] > maior) maior = v[p];
        if (v[p] < menor) menor = v[p];
    }
     
    
  
  
    return 0;
}