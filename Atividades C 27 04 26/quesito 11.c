// Online C compiler to run C program online
#include <stdio.h>

int main() {
    
   int valor=1, i=1, soma;

   while( i<=50){
      
      if (valor %2 != 0)
      soma++;
     {
     
  }
  valor++;
  i++;
   }
    printf("total de numeros impares: %d", soma);
    return 0;
}